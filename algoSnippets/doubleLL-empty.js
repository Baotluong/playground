class Node {
  constructor (val) {
  }
}

class DoublyLinkedList {
  constructor () {
  }

  push (value) {  
  }

  pop () {
  }

  shift () {
  }

  unshift (value) {
  }

  get (index) {
  }

  set (index, value) {
  }

  insert (index, value) {
  }

  remove (index) {
  }

  reverse () {
  }
}

const list = new DoublyLinkedList();
list.push(0);
list.push(1);
list.push(2);

console.log(list);
